# FRIDAY environment file — copy to .env and fill in.
#
#   PowerShell:  Copy-Item .env.example .env
#   Bash:        cp .env.example .env
#
# .env is gitignored (see .gitignore) — never commit it, never paste your
# key into a chat or a public log.
#
# Get an API key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Get an API key at https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=sk_your-key-here
